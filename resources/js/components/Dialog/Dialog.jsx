import React from "react";

const Dialog = (props) => {
    // const [visibleDialog, setVisibleDialog] = useState(false);
    return (
        <>
            {/* <section className="flex items-center justify-center w-screen h-screen bg-black z-40 fixed top-0 left-0 bg-opacity-75"> */}
                {/* <main className="container max-w-5xl p-10 bg-white rounded-lg"></main> */}
            {/* </section> */}
        </>
    );
};

export default Dialog;
